@using BussinessObject;
<html>

<head>
    <!-- Load Base -->
    @RenderPage("_Base.cshtml")
</head>

<body>
    <!-- Content -->
    <div class="container">
        <div class="row">
            <div class="col s12">
                @{
                    if (Session["UserID"] != null)
                    {
                        <p>session user id: @Session["UserID"]</p>
                    }
                }
                <p class="flow-text">Some info about the Debate</p>
            </div>
        </div>
    </div>

</body>

</html>